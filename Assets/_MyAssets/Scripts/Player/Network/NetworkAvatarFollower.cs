using Fusion;
using UnityEngine;

public class NetworkAvatarFollower : NetworkBehaviour
{
    //[Header("Player Channel")]
    //[SerializeField] private NetworkPlayerChannel playerChannel;

    //private Transform body;
    //private bool isReady;

    //public void SetBody(Transform bodyTransform)
    //{
    //    body = bodyTransform;
    //    isReady = true;
    //}

    //private void Start()
    //{
    //    if (Object.HasStateAuthority)
    //        playerChannel.OnAvatarRequested()=> 
    //}

    //private void Update()
    //{
    //    if (!isReady || !Object.HasStateAuthority) return;

    //    transform.SetPositionAndRotation(body.position, body.rotation);
    //}
}
